import Styled from 'styled-components';

export const Search = Styled.input`
  border: none;
  border-bottom: #f0f0f0 solid 1px;
  font-size: 1.6rem;
  font-weight: 100;
  padding: 1rem;

  :focus { 
    font-weight: 300
  }
`;
